import { FC } from 'react';

import { Container } from '@/styled';

import Layout from '../Layout';

const Feed: FC = () => {


	return (
		<Container>
			<Layout />
		</Container>
	);
};

export default Feed;
